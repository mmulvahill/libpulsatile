% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simulate.R
\name{simulate_pulse_population}
\alias{simulate_pulse_population}
\title{Simulate a multi-subject pulsatile hormone dataset}
\usage{
simulate_pulse_population(
  n_subjects = 5,
  num_obs = 144,
  interval = 10,
  mass_mean = 3.5,
  width_mean = 35,
  baseline = 2.6,
  halflife = 45,
  mass_sd = 1,
  width_sd = 5,
  ipi_mean = 12,
  ipi_var = 40,
  mass_mean_sd = 0,
  width_mean_sd = 0,
  baseline_sd = 0,
  halflife_sd = 0,
  seed = NULL
)
}
\arguments{
\item{n_subjects}{Number of subjects to simulate.}

\item{num_obs}{Number of observations per subject. Window duration is
\code{num_obs * interval}.}

\item{interval}{Time in minutes between observations.}

\item{mass_mean, width_mean}{Population mean pulse mass and width.}

\item{baseline, halflife}{Population mean baseline and half-life. When
\code{halflife_sd > 0}, \code{halflife} must exceed 8 (the model's minimum
half-life), otherwise the subject-level rejection sampler cannot converge.}

\item{mass_sd, width_sd}{Pulse-to-pulse SD of mass and width (within subject).}

\item{ipi_mean, ipi_var}{Inter-pulse interval mean and variance.}

\item{mass_mean_sd, width_mean_sd}{Subject-to-subject SD of the mean mass and
mean width (0 = identical subjects).}

\item{baseline_sd, halflife_sd}{Subject-to-subject SD of baseline and
half-life (0 = identical subjects).}

\item{seed}{Optional RNG seed for reproducibility.}
}
\value{
An object of class \code{population_sim}: a list with \code{data} (a
  list of per-subject data frames with \code{time} and \code{concentration},
  ready for \code{fit_pulse_population}), \code{n_subjects}, and \code{truth}
  (the population means and all SD arguments used, for comparison against
  recovered posteriors).
}
\description{
\code{simulate_pulse_population} generates data for the
  hierarchical population model: \code{n_subjects} independent pulsatile
  series, each produced by \code{\link{simulate_pulse}}. Subject-level means
  may vary around the population means via the \code{*_sd} arguments (set them
  to 0, the default, for identical subjects). The result plugs directly into
  \code{\link{fit_pulse_population}} as its \code{data} argument.
}
\examples{
sim <- simulate_pulse_population(n_subjects = 4, seed = 1)
length(sim$data)
}
\seealso{
\code{\link{simulate_pulse}}, \code{\link{fit_pulse_population}}
}
\keyword{pulse}
\keyword{simulation}
